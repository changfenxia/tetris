{
  "name": "statham-tetris",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@vitejs/plugin-svelte": "^2.0.0",
    "gh-pages": "^6.0.0",
    "svelte": "^4.0.0",
    "vite": "^4.0.0"
  }
}
